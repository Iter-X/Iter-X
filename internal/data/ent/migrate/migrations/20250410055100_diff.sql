-- Create "trip_poi_pool" table
CREATE TABLE "trip_poi_pool" ("id" uuid NOT NULL, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "updated_by" uuid NOT NULL, "created_by" uuid NOT NULL, "notes" character varying NULL, "poi_id" uuid NOT NULL, "trip_id" uuid NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "trip_poi_pool_points_of_interest_trip_pools" FOREIGN KEY ("poi_id") REFERENCES "points_of_interest" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION, CONSTRAINT "trip_poi_pool_trips_poi_pool" FOREIGN KEY ("trip_id") REFERENCES "trips" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
